{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "mcp-hub.fullname" . }}
  labels:
{{ include "mcp-hub.labels" . | indent 4 }}
spec:
  podSelector:
    matchLabels:
{{ include "mcp-hub.selectorLabels" . | indent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
{{ include "mcp-hub.selectorLabels" . | indent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.web.port }}
        - protocol: TCP
          port: {{ .Values.api.port }}
        - protocol: TCP
          port: {{ .Values.gateway.port }}
        - protocol: TCP
          port: {{ .Values.worker.port }}
    - from:
        - namespaceSelector:
{{ toYaml .Values.networkPolicy.ingressNamespaceSelector | indent 12 }}
      ports:
        - protocol: TCP
          port: {{ .Values.web.port }}
        - protocol: TCP
          port: {{ .Values.api.port }}
        - protocol: TCP
          port: {{ .Values.gateway.port }}
    {{- if and .Values.serviceMonitor.enabled .Values.worker.enabled .Values.worker.service.enabled }}
    - from:
        - namespaceSelector:
{{ toYaml .Values.serviceMonitor.networkPolicy.namespaceSelector | indent 12 }}
          {{- with .Values.serviceMonitor.networkPolicy.podSelector }}
          podSelector:
{{ toYaml . | indent 12 }}
          {{- end }}
      ports:
        - protocol: TCP
          port: {{ .Values.worker.port }}
    {{- end }}
  egress:
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    {{- if .Values.networkPolicy.allowExternalEgress }}
    - {}
    {{- end }}
{{- end }}
