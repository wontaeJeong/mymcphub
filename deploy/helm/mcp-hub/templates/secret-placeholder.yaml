apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mcp-hub.fullname" . }}-secret-placeholder
  labels:
{{ include "mcp-hub.labels" . | indent 4 }}
  annotations:
    mcp-hub.example.com/description: "Create external Secrets for DATABASE_URL, REDIS_URL, and OIDC_CLIENT_SECRET before installing workloads."
type: Opaque
