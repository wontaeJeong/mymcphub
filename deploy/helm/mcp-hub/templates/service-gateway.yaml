{{- if .Values.gateway.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-hub.componentName" (dict "root" . "component" "gateway") }}
  labels:
{{ include "mcp-hub.componentLabels" (dict "root" . "component" "gateway") | indent 4 }}
spec:
  type: {{ .Values.gateway.service.type }}
  selector:
{{ include "mcp-hub.componentSelectorLabels" (dict "root" . "component" "gateway") | indent 4 }}
  ports:
    - name: http
      port: {{ .Values.gateway.service.port }}
      targetPort: http
{{- end }}
