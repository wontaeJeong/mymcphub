# GitOps

GitOps base and environment overlays for MCP Hub will be added in the deployment phase.

Prompt-07 includes `stdio-adapter.example.yaml` as a scoped example for one adapter-backed MCP server. It is intentionally not a full environment overlay; prompt-10 will expand the Helm/GitOps system.
