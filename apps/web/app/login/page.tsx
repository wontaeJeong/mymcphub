import { loginCopy } from "../../lib/copy";

export default function LoginPage(){ return <section className="panel"><h1>{loginCopy.title}</h1><p className="muted">{loginCopy.description}</p></section>; }
