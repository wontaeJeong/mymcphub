{
  "name": "@mcp-hub/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "tsc -p ../../packages/ui/tsconfig.json && next build",
    "start": "next start",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "tsc -p ../../packages/ui/tsconfig.json && vitest run --passWithNoTests",
    "typecheck": "tsc -p ../../packages/ui/tsconfig.json && tsc -p tsconfig.json --noEmit",
    "test:unit": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@mcp-hub/ui": "workspace:*",
    "next": "^15.3.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
