# MCP Hub Web

Read-only Next.js catalog UI for the MVP. Active routes are `/catalog`, `/servers/[serverId]`, `/login`, and `/forbidden`.

The Web UI does not register, edit, delete, execute tools, generate client config, or request access. Server management is performed with `mcphubctl` through the API.
