# MCP Hub Web

Read-only Next.js catalog UI. Active routes are `/catalog`, `/servers/[serverId]`, `/login`, and `/forbidden`.

The Web UI does not register, edit, delete, execute tools, generate connection settings, or request access. Server management is performed with `mcphubctl` through the API.
