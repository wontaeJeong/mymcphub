{
  "name": "@mcp-hub/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:integration": "vitest run src/index.test.ts"
  },
  "dependencies": {
    "@mcp-hub/auth": "workspace:*",
    "@mcp-hub/logger": "workspace:*",
    "@mcp-hub/mcp-protocol": "workspace:*",
    "@mcp-hub/policy": "workspace:*",
    "prom-client": "^15.1.3"
  }
}
