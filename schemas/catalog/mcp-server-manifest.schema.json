{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MCP Server Manifest",
  "type": "object",
  "required": ["slug", "displayName", "ownerTeamId", "environment", "transport", "implementationLanguage"],
  "properties": {
    "slug": { "type": "string", "pattern": "^[a-z0-9-]+$" },
    "displayName": { "type": "string", "minLength": 1 },
    "description": { "type": "string" },
    "ownerTeamId": { "type": "string", "format": "uuid" },
    "environment": { "enum": ["dev", "stg", "prod", "shared"] },
    "transport": { "enum": ["streamable_http", "sse_legacy", "stdio_adapter", "external"] },
    "upstreamUrl": { "type": "string", "format": "uri" },
    "enabled": { "type": "boolean", "default": true },
    "riskLevel": { "enum": ["low", "medium", "high", "critical"], "default": "low" },
    "implementationLanguage": { "enum": ["go", "python"] },
    "tools": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "inputSchema"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "description": { "type": "string" },
          "enabled": { "type": "boolean", "default": true },
          "riskLevel": { "enum": ["low", "medium", "high", "critical"], "default": "low" },
          "inputSchema": { "type": "object" },
          "outputSchema": { "type": "object" }
        }
      },
      "default": []
    }
  }
}
