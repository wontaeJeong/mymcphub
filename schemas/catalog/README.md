# Catalog Schemas

`mcp-server-manifest.schema.json` validates first-party MCP server catalog manifests. It is a hand-authored source contract and is parsed by the schema check script.

Validate catalog and JSON Schema files from the repository root:

```sh
scripts/gen/schemas.sh --check
```
