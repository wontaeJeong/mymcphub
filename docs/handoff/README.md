# Handoff Archive

This directory contains historical lane handoff notes, conflict context, and future integration breadcrumbs from parallel implementation work.

These files are not the canonical source for current product behavior. Use the top-level docs, especially [Architecture](../ARCHITECTURE.md), [Control Plane API](../API.md), [Contracts](../CONTRACTS.md), and [Testing](../TESTING.md), for current behavior.

Do not bulk-delete or rewrite handoff files during cleanup without an explicit retention policy. Historical references may point to previous file names or schema locations; treat those references as archive context unless a current doc or active source file also depends on them.
